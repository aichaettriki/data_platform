# --- Manipulation de données (versions fixées pour éviter les calculs de pip) ---
pandas
numpy
pyarrow
fastparquet
openpyxl

# --- Cloud & Storage ---
boto3
s3fs
minio

# --- Base de données ---
# psycopg2-binary est déjà présent, mais on le garde pour être sûr
psycopg2-binary 
sqlalchemy

# --- Spark & Airflow Providers ---
# IMPORTANT : Version de pyspark DOIT être identique à la version binaire (3.5.1)
pyspark
apache-airflow-providers-apache-spark
apache-airflow-providers-celery
apache-airflow-providers-openlineage
apache-airflow-providers-postgres
apache-airflow-providers-redis

# --- Utils ---
loguru
python-dotenv
httpx
ujson
python-dateutil

# --- Monitoring ---
prometheus-client
prometheus-api-client

# --- Suppression des inutiles ---
# flask, requests, celery, redis : DEJA INCLUS dans l'image de base Airflow.
# openlineage-airflow : Remplacé par le provider officiel ci-dessus.